class AppendOnlyViolationError(Exception):
    """
    Phase 8 §1: adaptive_decisions is listed under "Append-Only (لا حذف
    أبدًا)". Application-layer enforcement, same pattern used for
    Evidence in TASK 5.
    """